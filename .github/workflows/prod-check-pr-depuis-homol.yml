name: 🔒 PR de homol vers main

on:
  workflow_call:
    inputs: {}
    secrets: {}

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  gate:
    name: 🔒 Verifier la source de la PR vers main
    runs-on: ubuntu-latest

    steps:
      - name: 🔎 Controle de la branche source
        id: check
        shell: bash
        run: |
          echo "### Contrôle PR" >> "$GITHUB_STEP_SUMMARY"
          echo "- Branche source (head): \`${{ github.head_ref }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Branche cible (base):  \`${{ github.base_ref }}\`" >> "$GITHUB_STEP_SUMMARY"

          if [ "${{ github.base_ref }}" = "main" ] && [ "${{ github.head_ref }}" != "homol" ]; then
            MSG="🚫 PR refusée : seules les PR de \`homol\` vers \`main\` sont autorisées."
            echo "$MSG"
            echo "$MSG" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          OK="✅ PR valide : ${{ github.head_ref }} → ${{ github.base_ref }}"
          echo "$OK"
          echo "$OK" >> "$GITHUB_STEP_SUMMARY"

      - name: 💬 Commenter la PR si invalide
        if: ${{ failure() }}
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            🚫 **PR refusée**

            - **Cible** : `${{ github.base_ref }}`
            - **Source** : `${{ github.head_ref }}`

            Seules les PR provenant de **`homol`** peuvent être fusionnées dans **`main`**.
            Merci d'ouvrir une PR **homol → main**.
