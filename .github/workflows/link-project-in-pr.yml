name: 🔗 Lier le projet a la PR

on:
  workflow_call:
    inputs: {}
    secrets: {}

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  link:
    runs-on: ubuntu-latest

    steps:
      - name: Detect ticket and ensure PR references it
        id: detect
        uses: actions/github-script@v7
        env:
          PR_KEYWORD: Closes
        with:
          result-encoding: string
          script: |
            const pr = context.payload.pull_request;
            const {owner, repo} = context.repo;

            const title = pr.title || "";
            const branch = pr.head?.ref || "";
            const body   = pr.body || "";

            const patterns = [
              /\[#?(\d+)\]/i,
              /(?:^|\s)#(\d+)(?=\s|$)/,
              /ticket[-_\/]?(\d+)/i
            ];
            let ticket = null;
            for (const rx of patterns) {
              const m = title.match(rx) || branch.match(rx) || body.match(rx);
              if (m) { ticket = parseInt(m[1],10); break; }
            }
            if (!ticket) {
              core.setFailed("Aucun numéro de ticket détecté (titre/branche/body).");
              return JSON.stringify({});
            }

            // Vérifier que l'issue existe
            await github.rest.issues.get({ owner, repo, issue_number: ticket });

            // Récupérer les node_id via GraphQL (format global requis)
            const query = `
              query($owner: String!, $repo: String!, $issueNumber: Int!, $prNumber: Int!) {
                repository(owner: $owner, name: $repo) {
                  issue(number: $issueNumber) {
                    id
                  }
                  pullRequest(number: $prNumber) {
                    id
                  }
                }
              }
            `;

            const result = await github.graphql(query, {
              owner,
              repo,
              issueNumber: ticket,
              prNumber: pr.number
            });

            const issueNodeId = result.repository.issue.id;
            const prNodeId = result.repository.pullRequest.id;

            // Garantir le lien PR -> issue
            const kw = process.env.PR_KEYWORD || "Closes";
            if (!new RegExp(`\\b#${ticket}\\b`).test(body)) {
              const newBody = (body ? body + "\n\n" : "") + `${kw} #${ticket}`;
              await github.rest.pulls.update({ owner, repo, pull_number: pr.number, body: newBody });
              core.info(`PR body mis à jour avec "${kw} #${ticket}".`);
            }

            // Retourner les node_id GraphQL
            return JSON.stringify({ issue_node_id: issueNodeId, pr_node_id: prNodeId });

      - name: ➕ Add ISSUE and PR to CORBIDEV
        uses: actions/github-script@v7
        env:
          PROJECT_ID: ${{ secrets.CORBIDEV_PROJECT_ID }}
          PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}
        with:
          github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
          script: |
            const result = JSON.parse('${{ steps.detect.outputs.result }}');
            const issueNodeId = result.issue_node_id;
            const prNodeId = result.pr_node_id;

            if (!issueNodeId || !prNodeId) {
              core.warning('Node IDs manquants, skip ajout au projet.');
              return;
            }

            // Récupérer l'ID du projet via son URL
            const projectQuery = `
              query($login: String!, $number: Int!) {
                user(login: $login) {
                  projectV2(number: $number) {
                    id
                  }
                }
              }
            `;

            const projectResult = await github.graphql(projectQuery, {
              login: 'LeZouzouEnWeb',
              number: 4
            });

            const projectId = projectResult.user.projectV2.id;
            core.info(`Project ID: ${projectId}`);

            // Ajouter l'issue au projet
            const addIssueMutation = `
              mutation($projectId: ID!, $contentId: ID!) {
                addProjectV2ItemById(input: {projectId: $projectId, contentId: $contentId}) {
                  item {
                    id
                  }
                }
              }
            `;

            try {
              await github.graphql(addIssueMutation, {
                projectId: projectId,
                contentId: issueNodeId
              });
              core.info(`✅ Issue ajoutée au projet`);
            } catch (e) {
              core.warning(`Issue déjà dans le projet ou erreur: ${e.message}`);
            }

            // Ajouter la PR au projet
            try {
              await github.graphql(addIssueMutation, {
                projectId: projectId,
                contentId: prNodeId
              });
              core.info(`✅ PR ajoutée au projet`);
            } catch (e) {
              core.warning(`PR déjà dans le projet ou erreur: ${e.message}`);
            }
