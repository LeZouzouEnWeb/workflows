# Exemple d'utilisation du workflow de liaison au projet GitHub
# Ce workflow ajoute automatiquement les PR et issues à un projet GitHub

name: 🔗 Lier au projet GitHub

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  link:
    uses: LeZouzouEnWeb/workflows/.github/workflows/link-project-in-pr.yml@main
    permissions:
      contents: read
      pull-requests: write
      issues: write
    secrets:
      # PAT avec permissions repository-projects pour ajouter au projet
      ADD_TO_PROJECT_PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}
    with:
      # URL du projet GitHub (à adapter selon votre configuration)
      project-url: https://github.com/users/VOTRE_USERNAME/projects/NUMERO
      # Mot-clé pour lier l'issue (Closes, Fixes, Resolves, etc.)
      PR_KEYWORD: Closes
